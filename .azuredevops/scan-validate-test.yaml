trigger:
  branches:
    include:
      - rg
      - main
      - release/*

resources:
  repositories:
    # - repository: templates
    #   type: git
    #   project: 'Pipelines'
    #   name: devops-templates
    #   ref: 'rg'
    - repository: templates
      type: github
      connection: 'futurecodeit'                    # ← Service connection name
      name: 'futurecodeit/devops-templates'      # ← GitHub owner/repo format
      ref: 'rg'

stages:
  - template: infra-pipeline-templates/terraform/azure-pipeline.yml@templates